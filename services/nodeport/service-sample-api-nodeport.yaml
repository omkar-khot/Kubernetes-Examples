apiVersion: v1
kind: Service
metadata:
  name: sample-api-nodeport
  namespace: apps-nonprod
  labels:
    app: sample-api
spec:
  type: NodePort
  selector:
    app: sample-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 32080
