# Ingress & Gateway Examples

This directory contains enterprise-ready examples for exposing Kubernetes Services:

- nginx/        # NGINX Ingress resources with TLS, security, routing
- gateway-api/  # Gateway API (Gateway, HTTPRoute) with multi-env setup

Use these as reference patterns for production-grade HTTP ingress.
